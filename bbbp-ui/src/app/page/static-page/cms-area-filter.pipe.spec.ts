import { CmsAreaFilterPipe } from './cms-area-filter.pipe';

describe('CmsAreaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CmsAreaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
