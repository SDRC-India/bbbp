import { DropDownFilterPipePipe } from './drop-down-filter-pipe.pipe';

describe('DropDownFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DropDownFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
